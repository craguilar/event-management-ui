/**
 * Main EventSummary object .
 * @export
 * @interface EventSummary
 */
export interface EventSummary {
  id: string;
  name: string;
  mainLocation: string;
  eventDay: string;
  timeCreatedOn?: string;
}
